This is a shim package that, when installed, installes downloads and installs redis to the same path as the python which is used to run it. It is recommended to only use this from a virtual environment. It also provides one api function that can be used to run redis.